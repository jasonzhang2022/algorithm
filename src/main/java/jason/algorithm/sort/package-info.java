/**
 * Practice the sort algorithm. 
 * In general Quick Sort is faster than Merge sorting since only O(1) memory is needed by Quick Sort.
 * This may have huge impact when input is bigger. 
 * @author jason.zhang
 *
 */
package jason.algorithm.sort;